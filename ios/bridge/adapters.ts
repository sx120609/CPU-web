// Reuse the deployed Web session adapter. Credentials remain inside WKWebView.
export * from "../../ios_next/bridge/adapters";
export { isIosNextNativeShell as isNativeScheduleShell } from "../../ios_next/bridge/adapters";
