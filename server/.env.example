PORT=3000
DATABASE_URL="file:./dev.db"
JWT_SECRET="cpu-web-dev-secret-please-change-in-production"
JWT_EXPIRES_IN="7d"
NODE_ENV=development

# 宿舍电费接口的 base URL。
# 默认 http://10.200.13.18:8899 仅校园网内可达；公网部署时改成 frp 穿透出来的地址。
# 不需要这个功能可以删掉，或在管理后台关闭 feature.electric 开关。
# DORM_ELECTRIC_BASE=http://10.200.13.18:8899
