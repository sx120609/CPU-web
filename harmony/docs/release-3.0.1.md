# HarmonyOS 3.0.1（21）发布包

包名 `cn.lizmt.cpuweb`。应用清单、入口参数、User-Agent 和 README 同步至 3.0.1（21）。包含此前已提交的鸿蒙导航安全区与平板布局调整；加载的网页功能取决于线上部署版本。

2026-09-10：100 项鸿蒙与共享桥回归测试通过，DevEco 6.1.1 release assembleApp 构建成功。沿用既有发布证书和 Profile，内层 HAP 与外层 APP 分别签名，均通过华为 verify-app 校验。重新读取最终包确认 release 模式、debug=false、版本 3.0.1、版本号 21，内嵌 HAP 与验签文件一致。

发布包：`output/harmony-3.0.1/cpuweb-harmony-3.0.1-v21-release.app`。

SHA-256：`be2920e822b3307eacc4c5b7548814795b56c4c3463838e4a7b8d7951bf8057a`。

未执行网站部署、AppGallery 上传或复审提交。未连接真机验证；本地签名验证不代表平台合法性校验或审核通过。备案、安全评估和新版截图仍按整改记录办理。历史包、签名材料和无关工作区文件保留，私钥、密码及包文件不纳入 Git。
